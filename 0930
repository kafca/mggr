Git 

1) make room for github files
mkdir =
echo 'folder' > 'file'

2) make skeleton
git init

3) track & staged
: git add

4) commit 
:git commit 

cf. 
-m ; message
-a ; omit add
-v ; difference

5) git rm
-- cached ''
\*~

6) vim editor
esc + :q  (quit)
    + :wq (save,quit) 
    + :q! (no-save,quit)
    

git remote add origin {"http://~"}
git push origin {"branch name"}

git checkout -b feature_x
git checkout master
git branch -d feature_x


git add
